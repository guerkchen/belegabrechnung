RewriteEngine On

# Let direct requests like /api/auth/login reach the PHP entrypoint
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php?route=/$1 [QSA,L]
